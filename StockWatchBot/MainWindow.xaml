<Window x:Class="StockWatchBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockWatchBot"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <!--<Grid>
            <oxy:Plot Title="{Binding Title}" Background="#00000000">
            <oxy:LineSeries ItemsSource="{Binding Points}"/>
        </oxy:Plot>
        </Grid>
        <Grid>
            <oxy:Plot Title="{Binding Title2}" Background="#00000000">
                <oxy:LineSeries ItemsSource="{Binding Points2}"/>
            </oxy:Plot>
        </Grid>-->
        <TextBlock HorizontalAlignment="Center" FontSize="32" Text="SPY"/>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" Text="{Binding Message}"/>
    </Grid>
</Window>
